# 플로팅 CTA 개편

`index.html` 한 파일만 수정했습니다. 기능 추가나 스크립트 변경은 없고, 하단 플로팅 버튼의 디자인·워딩만 바뀌었습니다.

## 왜 바꿨나

기존 플로팅 버튼은 우측 하단에 있고 잉크·보라·흰색 3색이 섞여 있어, 세 개가 서로 다른 위계로 읽혔습니다. 실제로는 셋 다 같은 성격의 진입점이라 한 톤으로 통일하고 위치를 가운데로 옮겼습니다.

## 무엇이 바뀌었나

| 항목 | 이전 | 이후 |
|---|---|---|
| 위치 | 우측 하단 (`right: 22px`) | 하단 가운데 (`left: 50%` + `translateX(-50%)`) |
| 색 | 잉크 / 보라 / 흰색 3색 | 잉크 캡슐 한 톤 |
| 인터랙션 | 버튼마다 다른 호버 | 호버·포커스 시 주황 `--orange`, 클릭 시 `#e63d29` |
| 폰트 | `font-weight: 800` | `font-weight: 450` |
| 라벨 | '바로가기' 텍스트 | 삭제 (버튼이 이미 역할을 설명함) |
| 구분 | 없음 | 버튼 사이 얇은 세로선 `.float-banner-sep` |
| 상태 표시 | 없음 | 첫 버튼 앞 주황 점 `.float-live` — 모집 중 신호 |
| 워딩 | 모집 행사 확인 / 협업/연사 문의 | 행사 신청 / 협업·연사 문의 |
| 모바일 | 가로 3분할 그리드 | 캡슐 유지, 터치 영역 세로 44px |
| 접근성 | 없음 | `:focus-visible`, `prefers-reduced-motion` 대응 |

## 수정 위치 (파일 내 4곳)

파일에 `[2026-09 수정]` 주석으로 표시해두었습니다.

1. **`<style>` 안 `/* FLOATING CTA */` 블록** — 전체 교체. 기존 `.float-banner-label`, `.float-banner-label::before`, `.float-banner-cta.event / .primary / .secondary` 규칙 삭제. `.float-banner-sep`, `.float-live`, 640px·380px 미디어 쿼리, `prefers-reduced-motion` 블록 추가.
2. **`@media (max-width: 900px)` 블록** — 플로팅 배너 관련 3줄 삭제 (새 규칙이 640px 기준으로 이동). `body { padding-bottom: 76px; }`는 유지.
3. **`<div class="float-banner">` 마크업** — 색상 클래스 제거, 라벨 삭제, 구분선·상태 점 추가, 워딩 변경.
4. **푸터 '연락하기' 목록과 CTA 섹션 `.btn-ghost`** — `협업/연사 문의` → `협업·연사 문의`. 슬래시가 '둘 중 하나'로 읽혀서 같은 폼 하나라는 점이 흐려집니다.

## 워딩을 바꾼 이유

'모집 행사'는 조직 입장의 표현이고, 앞의 주황 점이 이미 '모집 중'을 알리고 있어 의미가 겹쳤습니다. `행사 신청 / 멤버십 가입 / 협업·연사 문의`로 맞추면 세 버튼이 모두 `명사 + 행위` 구조가 됩니다.

링크는 그대로입니다. `행사 신청`은 여전히 event-us 목록 페이지로 갑니다.

## 확인한 것

- 데스크톱 가운데 정렬, 호버·클릭 주황 전환
- Tab 키 이동 시 동일한 주황 상태 노출
- 640px / 380px / 375px에서 세 문구가 한 줄 유지
- 모바일 버튼 높이 44px (iOS 최소 권장치)
- 사진·검정·흰색·보라 배경 위 대비

## 확인이 필요한 것

- 실제 히어로 이미지(`assets/image-01~03`)와 갤러리 이미지 위 대비
- `.scroll-sticky`, `.phrase-sticky`, `.brand-cta-sticky` 등 sticky 구간과 `z-index: 200` 충돌 여부
- 배포 후 360px 실기기에서의 줄바꿈

## 되돌리고 싶다면

버튼 폭이 좁게 느껴지면 640px 블록의 `padding: 0 0.62rem`과 `font-size: 0.75rem`만 키우면 됩니다. 세로 44px는 유지하는 편이 좋습니다.
